# ===========================================
# CLAUDE AUTHENTICATION (Choose ONE method)
# ===========================================

# OPTION 1: OAuth Authentication (Recommended)
# Obtain these by running 'claude login' on your local machine
# then copying the tokens from ~/.claude/credentials.json
CLAUDE_OAUTH_TOKEN=
CLAUDE_OAUTH_REFRESH_TOKEN=
CLAUDE_OAUTH_EXPIRES_AT=

# OPTION 2: API Key Authentication (Legacy)
# Only use if OAuth is not available
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===========================================
# REQUIRED
# ===========================================

# GitHub personal access token
# Generate at: https://github.com/settings/tokens
# Scopes: repo, read:org, workflow
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ===========================================
# GIT IDENTITY
# ===========================================

GIT_USER_NAME=Your Name
GIT_USER_EMAIL=you@example.com

# ===========================================
# STARTUP OPTIONS
# ===========================================

# Clone a repo on container start (optional)
# CLONE_REPO=owner/repo
# CLONE_REPO=https://github.com/owner/repo.git

# Run a task automatically on startup (optional)
# STARTUP_TASK=analyze this codebase and create documentation

# ===========================================
# CLAUDE CODE UI CREDENTIALS (REQUIRED)
# ===========================================

# Login credentials for the web UI
# IMPORTANT: Change these values before deployment!
# Generate a secure password with: openssl rand -base64 32
CLAUDE_UI_USER=
CLAUDE_UI_PASSWORD=

# ===========================================
# WEBHOOK API AUTH (optional)
# ===========================================

# Bearer token for webhook API
# If set, requests need: Authorization: Bearer <secret>
WEBHOOK_SECRET=

# ===========================================
# WEBHOOK API CONFIGURATION
# ===========================================

# Task timeout in milliseconds (default: 600000 = 10 minutes)
# TASK_TIMEOUT_MS=600000

# Maximum prompt length in characters (default: 50000)
# MAX_PROMPT_LENGTH=50000

# Rate limiting: max requests per minute per IP (default: 60)
# RATE_LIMIT_MAX=60

# Log level: error, warn, info, debug (default: info)
# LOG_LEVEL=info

# Task database path (default: /root/.claude-flow/tasks.db)
# TASK_DB_PATH=/root/.claude-flow/tasks.db

# Max tasks to keep in history (default: 1000)
# MAX_TASKS_HISTORY=1000

# OAuth client ID (default: claude-code)
# OAUTH_CLIENT_ID=claude-code

# ===========================================
# OPTIONAL
# ===========================================

# For Codex/OpenAI features in claude-flow
OPENAI_API_KEY=
